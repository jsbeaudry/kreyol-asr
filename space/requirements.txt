Cython
packaging
nemo_toolkit[asr] @ git+https://github.com/NVIDIA/NeMo.git@main
# NeMo's RNNT kernels predate numba 0.62's CUDA split; 0.66 breaks decoding with
# "Signature mismatch: 2 argument types given, but function takes 1".
numba==0.61.2
transformers>=5.13.0
soundfile
soxr
librosa
spaces
gradio>=5.0
